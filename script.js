// Typing Animation
var typed = new Typed(".typing",{
    strings:["WEB DEVELOPER"],
    typeSpeed:100,
    BackSpeed:60,
    loop:true
})
// Aside
const nav = document.querySelector(".nav"),
navList = nav.querySelectorAll
